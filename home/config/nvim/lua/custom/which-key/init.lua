-- NOTE: ここでキーマッピングを制御しない
local wk = require('which-key')
wk.setup {
  --  plugins = {
  --    marks = true,
  --    registers = true,
  --    spelling = { enabled = false, suggestions = 20, },
  --  },
  layout = {
    align = 'center',
  },
  --  ignore_missing = false,
}
